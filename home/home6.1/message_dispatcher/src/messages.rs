pub struct GreenMessage {
    pub text: String,
    pub counter: i32,
}

pub struct BlueMessage {
    pub value1: f64,
    pub value2: f64,
}

pub struct OrangeMessage {
    pub text1: String,
    pub text2: String,
    pub integer: i32,
    pub value: f64,
}
